# FitPlan RAG｜简历项目素材

> 面向普通健康成年人的中文循证健身问答与训练计划生成系统。项目重点不是“调用大模型”，而是把 RAG、Function Calling、安全审查、索引更新和生产工程链路完整串起来。

## 一句话版本

基于 Spring Boot 4 + Spring AI 2 + PostgreSQL/pgvector 构建双 Agent 循证健身规划平台，支持混合检索、工具调用、安全拦截、SSE 流式输出、持久化会话状态与容器化部署。

## 技术栈

Java 21、Spring Boot 4、Spring AI 2、Spring MVC、Reactor、PostgreSQL 16、pgvector、Flyway、Lucene BM25、Micrometer、Docker、Nginx、Vue 3、Vite、RAGAS。

## 推荐简历条目

### 后端 / Java 方向

- 设计并实现健身规划 Agent 与安全审查 Agent 的双 Agent 架构，通过 Function Calling 编排 9 个业务工具，覆盖知识检索、画像、训练日志和计划摘要。
- 构建 PostgreSQL + pgvector + Lucene BM25 混合检索链路，结合 RRF 融合、可选 Qwen Rerank 和上下文预算控制，支持 49 篇循证文档、约 1.8 万个知识块。
- 将用户画像、训练日志和计划摘要从进程内状态重构为 PostgreSQL 持久化，引入 Flyway 版本迁移和 JDBC Repository 抽象，保证重启后可恢复并支持多实例扩展。
- 将原先生成完整答案后一次性返回的伪流式链路重构为 Reactor 单次流式管线，避免失败重试重复执行写工具，并通过 `fetch + ReadableStream` 完成端到端 SSE。
- 设计用户账号会话 + Nginx IP 限流部署方案，使用多阶段 Docker 构建、健康检查和优雅停机，后端服务不直接暴露宿主机端口。
- 建立 Maven Surefire/Failsafe、JaCoCo、前端构建、镜像构建和 Gitleaks 的 CI 质量门禁。

### AI / RAG 方向

- 构建“语义检索 + 中文 BM25 + RRF 融合 + 可选重排”的混合 RAG 管线，降低单一向量检索在长尾术语和精确匹配上的召回缺失。
- 设计多层安全链路：确定性红旗规则、Prompt 注入检测、伤病动作禁忌和知识依据不足拒答均在模型前执行，LLM 风险分类器提供语义兜底，风险输入不进入模型或会话记忆。
- 实现增量索引与数据库租约锁，按内容哈希、Embedding 模型、向量维度和 Chunk Schema 判断是否重建；索引更新采用先写新版本再删除旧版本。
- 建立 RAGAS 四指标和 69 条业务评测集，覆盖检索召回、答案忠实度、相关性和安全边界。
- 为知识语料建立来源、许可、版权状态白名单校验，非授权文档在索引阶段直接拒绝。

### 全栈 / 工程化方向

- 使用 Vue 3 与原生流式响应实现可取消的聊天工作台，加入登录注册、Bearer Session、路由守卫和可切换会话标识，并补齐超时、断线和服务异常反馈。
- 通过统一 Problem Details、请求大小限制、请求 ID 和 MDC 日志提高接口可调试性与稳定性。
- 使用 Micrometer 为检索、索引、安全分类和 Agent 调用建立成功率、耗时及性能指标。

## 面试时可讲的三个难点

1. **为什么不能简单重试 Agent 请求**：Agent 会调用写工具；如果模型失败后自动重跑完整 Agent，可能重复保存训练日志。解决方式是单次流式执行、禁止隐式重试，并向用户明确提示。
2. **为什么索引更新必须先加后删**：先删除旧向量会让检索在重建期间返回空结果；先写新版本、成功后淘汰旧 ID，可以缩短数据不可用窗口。
3. **为什么安全审查必须在 Memory Advisor 之前**：否则包含红旗信息的消息可能已经进入会话记忆。Advisor 顺序决定了数据是否真正 fail-closed。

## 可量化指标（推荐写）

### 当前代码可复现

- 49 篇许可受控知识文档，约 18,386 个知识块。
- 2 个 Agent、9 个 Function Calling 工具。
- 40 条检索评测 + 29 条生成评测，共 69 条评测用例。
- 71 个后端单元测试；覆盖用户隔离、认证、Prompt 注入、禁忌规则和检索拒答。
- 安全模块 JaCoCo 行覆盖率约 92%，项目整体行覆盖率约 47%（以最新 mvn verify 为准）。
- 3 个 Flyway 迁移，覆盖 RAG、Agent 状态、用户账号和持久化聊天记忆。
- CI 包含 4 类门禁：后端测试与打包、前端生产构建、容器镜像构建、Secret Scan。
- 前端首屏 JS 约 108 KB，gzip 后约 42 KB。
- 本地 JAR 启动约 5 秒；21,001 块 BM25 索引重建约 5.6 秒（开发机数据，非压测结论）。

### 历史评测数据（可以写，但必须注明口径）

- 21 条 RAGAS 检索评测：Context Precision 0.80、Context Recall 0.69、Answer Relevancy 0.90。
- 21 条三通道消融：Lucene BM25 独立 Recall@2 为 0.43；混合检索答案覆盖@2 为 0.52，较纯词法 0.33 提升约 57%。
- 纯检索延迟：均值约 215 ms，203 次采样。
- 29 条生成评测：关键词平均覆盖率 0.874；安全类覆盖率 0.917；18/29 完全通过。
- 上述数据来自旧基线语料和旧实现，面试前应以最新语料和最终代码重新运行评测。

### 不建议直接写进简历

- 不要写“89 篇、21,001 块”作为当前规模；这是已清理语料前的历史基线。
- 不要写“准确率 90%”或“生产级”。项目没有临床准确率、并发压测和正式安全认证数据。
- 不要写 Faithfulness 0.24 作为优势；该指标受助手式追问话术影响，目前属于待优化项。
- 不要写“企业级 OIDC/JWT”；当前是 opaque session token 用户体系，浏览器会话存储，不是 SSO。
- 不要写端到端 p50 12.3 秒作为性能亮点；当前更适合表述为后续优化目标。

## 真实边界

- 当前是邮箱 + opaque session token 用户体系，不等价于企业 SSO/OIDC。
- 聊天记忆、用户画像、日志和计划均已持久化到 PostgreSQL，并按用户与会话隔离。
- 项目适合演示、课程设计、实习/校招简历和面试讨论；若真正面向患者，需要额外完成隐私、临床安全和法规评估。
